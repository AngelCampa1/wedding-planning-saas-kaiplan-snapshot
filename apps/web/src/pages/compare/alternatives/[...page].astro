---
export const prerender = true;
import "../../../styles/global.css";
import { getCollection } from "astro:content";
import CategoryHub from "@kaiplan/marketing/hubs/category-hub.astro";
import PublicSignupCta from "@kaiplan/marketing/components/public-signup-cta";
import { FilterChipsIsland } from "@kaiplan/marketing/components/filter-chips-island";
import { siteConfig } from "@/config/site";
import { hubFaqs } from "@/config/hub-faqs";
import {
  sortByUpdatedAtDesc,
  mapToContentItems,
} from "@kaiplan/marketing/lib/collections";
import { BUYER_STAGE_FILTER, DATE_SORT_OPTIONS } from "@/lib/content-filters";
import { filterIndexableEntries } from "@/lib/indexable-content";
import { getKaiplanAlternativesHubCopy } from "@/lib/hub-page-copy";
import type { GetStaticPathsOptions } from "astro";

export async function getStaticPaths({ paginate }: GetStaticPathsOptions) {
  const entries = filterIndexableEntries(await getCollection("alternatives"));
  const sorted = sortByUpdatedAtDesc(entries);
  const items = mapToContentItems(
    sorted,
    (e) => `/compare/alternatives/${e.data.competitor.slug}`,
    (e) => ({ competitor: e.data.competitor.name }),
  );
  return paginate(items, { pageSize: 10 });
}

const { page } = Astro.props;
const alternativesHubCopy = getKaiplanAlternativesHubCopy();
---

<CategoryHub
  config={siteConfig}
  title={alternativesHubCopy.title}
  description={alternativesHubCopy.description}
  items={page.data}
  page={page}
  basePath="/compare/alternatives/"
  breadcrumbs={[
    { label: "Home", href: "/" },
    { label: "Compare", href: "/compare/" },
    { label: "Alternatives", href: "/compare/alternatives/" },
  ]}
  faqs={hubFaqs["/compare/alternatives"]}
  faqHeading={alternativesHubCopy.faqHeading}
>
  <FilterChipsIsland
    slot="filters"
    client:load
    items={page.data}
    filters={[BUYER_STAGE_FILTER]}
    sortOptions={DATE_SORT_OPTIONS}
  />
  <section
    slot="cta"
    class="mt-8 py-10 text-center border-t border-[var(--color-neutral-200)]"
    id="signup"
  >
    <h2 class="text-2xl font-bold text-[var(--color-brand-text)] mb-2">
      {alternativesHubCopy.ctaHeading}
    </h2>
    <p class="text-[var(--color-brand-muted)] mb-6 max-w-md mx-auto">
      {alternativesHubCopy.ctaBody}
    </p>
    <PublicSignupCta
      apiUrl=""
      sourcePage="/compare/alternatives/"
      buttonText={alternativesHubCopy.ctaButtonText}
      ctaTarget={siteConfig.funnel.bofu.ctaTarget}
      surveyQuestions={siteConfig.survey.questions}
    />
  </section>
</CategoryHub>
